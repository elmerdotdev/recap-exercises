/**
 * Using the array that you created on ex02.js, apply the modifications listed below:
 * 
 * 1. Remove the last country in the array using pop
 * 2. Add a different country to the end of the array using push
 * 3. Insert two new countries between the 2nd and 3rd country by using splice
 * 
 * Print the array using a for loop
 */